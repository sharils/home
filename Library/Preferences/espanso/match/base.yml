# Automatically generated by espanso migration tool
# Original file: default.yml

matches:
  - trigger: p3,.
    replace: "{{d}}"
    vars:
      - name: d
        type: shell
        params:
          cmd: "pbpaste | fold -w3 | tr '\n' ' '"

  - trigger: pd,.
    replace: "{{d}}"
    vars:
      - name: d
        type: shell
        params:
          cmd: "pbpaste | tr -cd '[:digit:]'"

  - trigger: pl,.
    replace: "{{d}}"
    vars:
      - name: d
        type: shell
        params:
          cmd: "pbpaste | xargs"

  - trigger: pu,.
    replace: "{{d}}"
    vars:
      - name: d
        type: shell
        params:
          cmd: "pbpaste | tr '[:lower:]' '[:upper:]'"
