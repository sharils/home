# Automatically generated by espanso migration tool
# Original file: default.yml
---
matches:

  - trigger: p\d,.
    replace: "{{d}}"
    vars:
      - name: d
        type: shell
        params:
          cmd: "pbpaste | tr -cd '[:digit:]'"

  - trigger: p\l,.
    replace: "{{u}}"
    vars:
      - name: u
        type: shell
        params:
          cmd: "pbpaste | tr '[:upper:]' '[:lower:]'"

  - trigger: p\u,.
    replace: "{{u}}"
    vars:
      - name: u
        type: shell
        params:
          cmd: "pbpaste | tr '[:lower:]' '[:upper:]'"

  - trigger: pc,.
    replace: "{{d}}"
    vars:
      - name: d
        type: shell
        params:
          cmd: "pbpaste | wc -c"

