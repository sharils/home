# Alternately, if you would like to keep your custom scripts under version
# control, you can set BASH_IT_CUSTOM in your ~/.bashrc to another location
# outside of the $BASH_IT folder. In this case, any *.bash file under every
# directory below BASH_IT_CUSTOM folder will be used.
export BASH_IT_CUSTOM=~/.bash_it_custom
